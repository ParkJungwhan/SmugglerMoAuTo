<Border
    x:Class="MATMain.Views.LeftMainContent" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:MATMain.Views"
    mc:Ignorable="d" xmlns:hc="https://handyorg.github.io/handycontrol" Margin="16,16,0,16"
    hc:ScrollViewer.IsInertiaEnabled="True" Background="{DynamicResource RegionBrush}"
    CornerRadius="10" Effect="{StaticResource EffectShadow4}">
    <DockPanel>
        <Button  DockPanel.Dock="Top" HorizontalAlignment="Stretch" Command="{Binding AddItem}"
                Margin="10,10,10,4" Content="{Binding AddTitle}" d:Content="Add item" BorderThickness="0" Background="{DynamicResource SecondaryRegionBrush}" />

        <ListBox BorderThickness="0 1 0 0" Padding="0" ScrollViewer.CanContentScroll="False" Grid.Row="1" VirtualizingStackPanel.IsVirtualizing="True"
         SelectedIndex="{Binding SelectedIndex}" ItemsSource="{Binding ItemList}" SelectionMode="Single" Background="Transparent" Style="{StaticResource ListBoxTransparent}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <!--<Rectangle Width="16" Height="16" Fill="{Binding ImageBrush}" />-->
                        <!--SourceText="{ex:Lang Key={Binding Name}}"-->
                        <hc:HighlightTextBlock Margin="6,0,0,0" SourceText="{Binding Name}" QueriesText="{Binding QueriesText}" />
                        <Path Visibility="{Binding IsNew,Converter={StaticResource Boolean2VisibilityConverter}}" VerticalAlignment="Center"
                              Data="{StaticResource NewGeometry}" Stretch="Uniform" Margin="10,0,0,0" Width="30" Height="14" Fill="{DynamicResource AccentBrush}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.GroupStyle>
                <GroupStyle ContainerStyle="{StaticResource DemoGroupItemStyle}" />
            </ListBox.GroupStyle>
            <hc:Interaction.Triggers>
                <hc:EventTrigger EventName="SelectionChanged">
                    <hc:EventToCommand Command="{Binding SwitchitemCmd}" PassEventArgsToCommand="True" />
                </hc:EventTrigger>
            </hc:Interaction.Triggers>
        </ListBox>
    </DockPanel>
</Border>